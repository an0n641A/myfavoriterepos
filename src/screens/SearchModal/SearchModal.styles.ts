import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {},
  list: {
    padding: 5,
    marginTop: -5,
  },
  indicator: {
    marginTop: 25,
  },
});

export default styles;
